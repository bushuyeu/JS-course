// AND — both vars A and B should be TRUE to return true

// OR - either var A or B should be TRUE to return true

// NOT - inverts boolean value

var age = 18;
var lowerLimit = 16;
var upperLimit = 30;

console.log(age > lowerLimit AND age < upperLimit);
console.log(!(age > lowerLimit)); //fasle
