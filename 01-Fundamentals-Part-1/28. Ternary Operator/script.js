const age = 23;

//  

// declaire variables conditionally
const drink = age >= 18 ? 'wine' : 'water';
console.log(drink);


// conditional expressions inside of the ``:

console.log(`I like to drink ${age >= 18 ? 'wine' : 'water'}`);